{
  "buildCommand": "node node_modules/knex/bin/cli.js migrate:latest --knexfile server/src/db/knexfile.js && npm --prefix client run build",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "functions": {
    "api/index.js": { "memory": 256, "maxDuration": 10 }
  }
}
